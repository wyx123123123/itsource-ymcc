spring:
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 # 注册中心地址
        prefix: application-uaa # 配置前缀
        file-extension: yaml # 配置后缀
        group: DEFAULT_GROUP # 默认分组
        namespace: 53be3c4a-ffa2-42e4-b37c-cfb25c87c5f7 # 命名空间