spring:
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 # 注册中心地址
        prefix: application-gateway # 配置前缀
        file-extension: yaml # 配置后缀
        group: DEFAULT_GROUP # 默认分组
        namespace: d229e80b-a6c8-4e46-aeb2-82b73eed2587 # 命名空间